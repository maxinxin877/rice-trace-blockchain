# 开发环境覆盖配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qukuailian?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    # 本机 MySQL root 密码
    password: 123456
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

bcos:
  enabled: false
